/**
 * This module lets you easily get the root path for this project without having
 * to worry about relative paths.
 */

import nodePath from "node:path";
export default nodePath.resolve(__dirname);
